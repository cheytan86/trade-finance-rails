# Stack Rules — {{PRODUCT_NAME}}

<!-- GENERATED BY BOOTSTRAP. Every value below is written by reading this repo,
     never from memory, and every claim cites a file. Rename to STACK_RULES.md
     beside the kit folders once filled. The kits treat this file as the
     authority on stack facts; the repo wins over it where they disagree. -->

## The gate commands

The regression gate every phase runs. Measured at adoption — record the real
numbers, not "should pass":

```text
{{GATE_COMMANDS}}         e.g.  npm run lint · npx tsc --noEmit · npm test · npm run build
Baseline at adoption: {{BASELINE_NUMBERS}}   e.g. "lint 0 problems · 0 type errors · 852 tests · build exit 0"
If there is no test suite, say so here plainly — and never let a session claim tests passed.
```

## Framework rules

```text
Framework + version: {{FRAMEWORK_VERSION}}
Version quirks: {{QUIRKS}}   e.g. "newer than model training data — read <local docs path> before writing framework code"
Route-handler conventions: {{HANDLER_RULES}}   e.g. runtime, maxDuration, where env is read
```

## Data rules

```text
Data layer: {{DATA_LAYER}}   e.g. "Supabase Postgres + RLS; generated types at src/lib/.../database.types.ts"
The boundary rule: feature-folder fixtures are typed from the generated types
(import type only); ZERO live data-client imports inside the feature folder.
Audit command: {{AUDIT_COMMAND}}   e.g. grep -rn "supabase" src/features/<slug>/  → type imports only
Schema changes: only what a design's data contract names, re-approved before written.
```

## Permission pattern

```text
How pages/routes gate access in this app: {{PERMISSION_PATTERN}}
e.g. "getGrantedScreens + hasScreenAccess, two lines, see <file:lines>; ungranted users redirect to <route>"
```

## Secrets and keys

```text
Model API key: {{KEY_NAME}} in {{ENV_FILE}}, server-side, read at request time.
Never public-prefixed, never in source, never echoed, never added to the host
platform during Develop.
Legitimately public env vars in this app (do not let their pattern bleed onto
secrets): {{PUBLIC_VARS}}
```

## Host platform and deploy posture

```text
Host: {{HOST_PLATFORM}}   e.g. "Vercel, production deploys from main"
Push side effects: {{PUSH_EFFECTS}}   e.g. "any branch push creates a preview deployment"
Env scoping model: {{ENV_SCOPES}}   e.g. "Preview scope exists; public vars are build-time inlined — redeploy after changes"
```

## The untouchables

Files no feature may modify, ever, without a explicit decision (each with why):

```text
{{UNTOUCHABLES}}   e.g. "vercel.json (live cron) · supabase/migrations · generated types · auth in any form"
```

## High-stakes surfaces

What in this product moves money, makes consequential decisions, or messages
out — the places a human gate is mandatory:

```text
{{HIGH_STAKES}}
```

## The smoke path candidates

The two or three flows that must never break, clickable in thirty seconds
(Gate 0.5 picks from these per feature):

```text
{{SMOKE_PATHS}}
```
