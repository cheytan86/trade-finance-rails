---
description: Start the Discovery phase for one new feature (phase 1 of 5)
argument-hint: <feature idea, or a product area to explore>
---

I am starting the **Discovery** phase for one feature of my {{PRODUCT_NAME}} product. My idea or area of interest: **$ARGUMENTS**

Read `discovery-kit/AGENTS.md` and follow it exactly, together with `discovery-kit/01_DISCOVERY.md` — AGENTS.md carries the session rules; follow them with the feature above filled in.

Key rules (the kit has the rest): I choose the feature — never generate a roadmap or feature list (an area yields at most 2–3 candidates for me to pick). One question at a time. Every codebase claim cites a file. Output goes to `docs/product/<feature-slug>/discovery.md` (see `docs/product/README.md` for naming). Stop at the end of Discovery and tell me the next phase is Design.

At the end of the Step 2 grill, apply the weight test from PROCESS_GUIDE.md (model calls / money / schema / auth / many files). If the feature is LIGHT, say so and finish it in THIS session as a brief per FEATURE_BRIEF_TEMPLATE.md — one file, docs/product/<feature-slug>/brief.md — instead of the 10-row discovery file. Otherwise continue the full discovery.

Start with Step 0: verify `discovery-kit/YOUR_PRODUCT.md` against the repo, then engage with my idea above.
